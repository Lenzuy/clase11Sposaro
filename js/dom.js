//debugger;
let tituloCarrito = "";

// Según las elecciones del Usuario en app.js, se generará un título de Carrito y un botón (sin función de momento) para proceder al pago.
/* if (carritoLleno === true) {
  document.getElementById("tituloCarrito").innerText =
    "Tiene items en su Carrito 🛒";
  let hr = document.createElement("hr");
  document.getElementById("tituloCarrito").append(hr);
  let button = document.createElement("button");
  document.getElementById("tituloCarrito").append(button);
  button.innerHTML = "Proceda al pago";
} else {
  document.getElementById("tituloCarrito").innerText = "Su Carrito está vacío";
  let hr = document.createElement("hr");
  document.getElementById("tituloCarrito").append(hr);
} */

// Este código fue integrado en la función cantidadItemd(), declarada en app.js
